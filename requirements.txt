duckdb>=1.5
numpy>=2.0
# Reads the basketball .xlsx workbooks and writes the Excel exports. Missing
# here until now, which meant the scheduled run's test step failed to collect
# three files rather than running them - including the one test that proves the
# port reproduces Ben's published ratings.
openpyxl>=3.1
pandas>=2.2
# Downscales cached logos. ESPN serves 500px and ignores size hints, so 365
# basketball marks would otherwise be 23MB in the repository.
pillow>=10.0
pyarrow>=18.0
requests>=2.32
scipy>=1.14
xlrd>=2.0
pytest>=8.0
